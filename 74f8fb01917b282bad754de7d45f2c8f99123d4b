Этап 3 плана prd/plan-store-item-time-series-analysis.md, issues #11–#14.

Построитель src/report/report_tables.py: разрез, итог и доли одним запросом
(GROUPING SETS + FORMAT Markdown). Правила держит сам, нарушение — ValueError:
- Branch обязан быть в ключе;
- Scope и OutlierStore (в разрезе по магазину) добавляются в ключ сами;
- Scope нельзя отфильтровать условием;
- источник с Variant требует его в ключе или в условии (колонка ищется целым
  словом: VariantDays вариант не ограничивает);
- доли — из сумм на каждом уровне группировки.
Каждая таблица пишется в манифест analysis/report_tables.json (для этапа 14).
Модуль назван не tables: его перекрыл бы пакет PyTables.

Сборка src/report/series_report.py → docs/series_analysis.md:
- шапка: дата расчёта и отпечаток данных (период, файлы, строки, sum(cityHash64(*)));
- раздел «Популяция»: пары и выручка по когортам; класс спроса и дефицит
  по вариантам ряда; пары без класса спроса по причинам;
- автоматические куски между <!-- auto:имя --> и <!-- /auto -->, текст вне них
  не трогается; сломанная разметка роняет шаг, не тронув файл;
- у run() нет умолчания для пути отчёта: docs/ не уводится DF_DATA_DIR;
- --checks сверяет манифест, разделы и период в шапке с профилем.

settings.py: новый каталог paths.docs (ключ docs в config.yaml).
sql.py: константы SCOPE_MAIN / SCOPE_DISPUTED — одно написание для слоя и отчёта.

Полный прогон main.py --checks на выгрузке 2025-01-01…2026-08-31: 20 с,
из них отчёт 1.4 с.
