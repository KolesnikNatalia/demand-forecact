Было: <слой>_checks_<ГГГГММДД_ЧЧММСС_мкс>.parquet и .md на каждый прогон —
при 2–3 прогонах в день каталог data/checks зарастал бы тысячами файлов,
среди которых не найти текущий. Решение 2026-09-20: нужен последний результат,
а не история. Время прогона остаётся в колонке RunAt и в заголовке отчёта.

Стало: <слой>_checks.parquet и .md, прогон перезаписывает. Отчёт .md удаляется
до запуска: иначе упавший запрос проверок оставлял бы рядом отчёт прошлого
прогона «Замечаний нет» (найдено ревью этапа 3, 2026-09-22).

Тесты: журнал перезаписывается, а не копится; упавший запрос не оставляет
старого отчёта. 16 старых файлов с метками времени удалены из data/checks
(вне git).
