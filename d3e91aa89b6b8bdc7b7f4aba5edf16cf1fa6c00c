Журнал, отчёт рядом с ним, вывод в лог и остановка прогона были частью проверок слоя дневных рядов. Этап 2 скопировал бы их целиком, и так поступил бы каждый следующий шаг.

Теперь шаг приносит две части своего SQL — определения CTE и union all из строк проверок, — а всё остальное делает checks_journal.run(имя, заголовок, preamble, body, paths, текст ошибки).

Пустой журнал теперь роняет прогон: ни одна величина не сверена, а «ошибок 0» выглядело бы как успех.
